# spell-checker:disable
# Check for outdated:          uv tree --outdated --depth 1
# Upgrade within constraints:  uv lock --upgrade

[build-system]
requires = ["setuptools>=80.0.0", "setuptools_scm", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-optimizer"
dynamic = ["version"]
description = "Oracle AI Optimizer and Toolkit"
requires-python = ">=3.11,<3.13"
license =  "UPL-1.0"
authors = [
    {name = "Oracle", email = "obaas_ww@oracle.com"}
]

# - common dependencies that are always needed
dependencies = [
    "langchain==1.3.14",
    "httpx==0.28.1",
    "oracledb~=4.0",
    "matplotlib==3.11.1",
    "pydantic-settings==2.15.0"
]

[project.optional-dependencies]
# Server component dependencies
# - server: server side only
server = [
    "bokeh==3.9.2",
    "datasets>=5.0.1",
    "docling==2.119.0",
    "evaluate>=0.4.6",
    "faiss-cpu==1.15.0",
    "fastmcp==3.4.7",
    "giskard==2.19.2",
    "langchain-litellm==0.7.1",
    "langchain-mcp-adapters==0.3.2",
    "langchain-oracledb==1.5.0",
    "litellm==1.96.0",
    "llama-index-core==0.14.24",
    "oci~=2.0",
    "pyagentspec==26.1.2",
    "pypdf==6.16.1",
    "umap-learn==0.5.12",
    "torch", # Direct requirement to select the CPU-only PyTorch distribution for Docling.
    "torchvision", # Direct requirement to select the CPU-only PyTorch distribution for Docling.
]

# - client: client side only
client = [
    "streamlit==1.61.1",
]

# - otel: opentelemetry collector for server-side observability
otel = [
    "opentelemetry-distro==0.65b0",
    "opentelemetry-exporter-otlp==1.44.0",
    "opentelemetry-instrumentation-fastapi==0.65b0",
    "opentelemetry-instrumentation-httpx==0.65b0",
    "opentelemetry-instrumentation-logging==0.65b0",
    "opentelemetry-instrumentation-requests==0.65b0",
    "openinference-instrumentation-langchain==0.1.70"
]

# - all: all-in-one installation (server (langgraph) + client)
all = [
    "ai-optimizer[server]",
    "ai-optimizer[client]",
    "ai-optimizer[otel]",
]

[dependency-groups]
# Development-only tooling. Install explicitly with `uv sync --group dev`.
dev = [
    "bandit",
    "docker",
    "mypy",
    "types-psutil",
    "pandas-stubs",
    "ruff",
    "pyright[nodejs]",
    "pytest-asyncio",
    "pytest-cov",
    "types-jsonschema",
    "yamllint"
]

[tool.uv]
# Application installs should not include development tooling.
default-groups = []
# Ensure packages without source distributions are installable on every supported platform.
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'linux' and platform_machine == 'aarch64'",
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
    "sys_platform == 'win32' and platform_machine == 'AMD64'",
]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
torchvision = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.setuptools]
# ``src/`` is the package root; the wheel ships its contents at the top
# level (``server``, ``client``, plus the bare-name modules below) so
# runtime imports like ``from server.app...`` resolve directly.
py-modules = ["_version", "entrypoint", "logging_config", "logging_redaction", "net_addressing", "runtime_config_fields", "url_safety"]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false
include = ["server*", "client*"]

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
addopts = "--import-mode=importlib"
# anyio is the single async-test backend (see the root conftest.py). ``anyio_mode =
# auto`` makes anyio claim every coroutine test and its async fixtures at collection
# time, with no per-test marker. pytest-asyncio stays installed but pinned to
# ``strict`` so it only ever acts on the (unused) ``@pytest.mark.asyncio`` marker,
# never auto-claiming async tests/fixtures and racing anyio for the event loop.
anyio_mode = "auto"
asyncio_mode = "strict"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::RuntimeWarning:litellm",
    "ignore::requests.exceptions.RequestsDependencyWarning",
    ]
markers = [
    "unit: Unit tests (mocked dependencies, fast execution)",
    "integration: Integration tests (real components, may require external services)",
    "slow: Slow tests (deselect with '-m \"not slow\"')",
    "db: Tests requiring Oracle database container (deselect with '-m \"not db\"')",
    "live_oci: Live tests against OCI GenAI (needs AIO_GENAI_COMPARTMENT_ID + AIO_GENAI_REGION; auto-skips otherwise)",
]
norecursedirs = ["tmp", "build"]

[tool.pyright]
pythonVersion = "3.12"
extraPaths = ["src"]
exclude = [
    "**/.*",
    "**/.venv",
    "**/__pycache__",
    "**/node_modules", 
    "build",
    "src/client/source",
    "tmp/"
]

[tool.ruff]
line-length = 120
target-version = "py312"
src = ["src"]
extend-exclude = ["**/wip/**", "src/client/mcp", "docs/themes/relearn", "docs/public", "docs/static/demoware", "docs/demo"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "F",      # pyflakes
    "W",      # pycodestyle warnings
    "I",      # isort
    "PL",     # pylint (PLC, PLE, PLR, PLW)
    "SIM",    # flake8-simplify
    "SLF",    # flake8-self (protected-access)
    "ARG",    # flake8-unused-arguments
]
ignore = [
    "BLE001",  # broad-exception-caught
    "PLR2004", # magic-value-comparison
    "SIM102",  # collapsible-if
]

[tool.ruff.lint.pylint]
max-args = 10
max-branches = 15
max-locals = 16
max-returns = 6
max-statements = 50
max-nested-blocks = 6
max-bool-expr = 6

[tool.ruff.lint.per-file-ignores]
"src/client/source/**" = ["ALL"]
"**/tests/**" = [
    "E402",     # module-import-not-at-top-of-file
    "SLF001",   # protected-access
    "PLC0415",  # import-outside-toplevel
    "ARG001",   # unused-function-argument
    "ARG002",   # unused-method-argument
    "ARG005",   # unused-lambda-argument
    "PLR0913",  # too-many-arguments
    "PLR0915",  # too-many-statements
    "PLR2004",  # magic-value-comparison
]

[tool.ruff.format]
quote-style = "double"

[tool.bandit]
# Run as: bandit -c pyproject.toml -r .
exclude_dirs = [".venv", "tests", "src/tests", "src/client/tests", "src/server/tests", "build", "docs", "opentofu"]

[tool.setuptools_scm]
root = "."
fallback_version = "0.0.0"
