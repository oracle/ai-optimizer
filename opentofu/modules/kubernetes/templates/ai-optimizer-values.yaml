# Copyright (c) 2024, 2026, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.
# spell-checker: disable

global:
  env: "iac"
  cleanupPVCs: true
  api:
    secretName: "ai-optimizer-api-key-${label}"
%{ if ocir_url != "" ~}
  imageRegistry: "${ocir_url}"
%{ endif ~}

# -- API Server configuration
server:
  image:
    repository: ${repository_base}/ai-optimizer-server
    tag: "${app_version}"
    pullPolicy: Always

  ingress:
    enabled: true
    className: native-ic
    annotations:
      oci-native-ingress.oraclecloud.com/healthcheck-protocol: HTTP
      oci-native-ingress.oraclecloud.com/healthcheck-path: "/v1/readiness"
      oci-native-ingress.oraclecloud.com/http-listener-port: "80"
  service:
    type: "NodePort"

  # -- Enable Workload Identity
  ociConfig:
    oke: true
    region: "${oci_region}"

  # -- OpenTelemetry export to the in-cluster SigNoz collector.
  # Endpoint auto-populates from signoz.enabled below; otel must follow
  # signoz.enabled because the chart validator rejects otel-on with no
  # endpoint.
  otel:
    enabled: ${is_observability_enabled}
    insecure: true

  # -- Oracle Database Configuration
  database:
%{ if db_type == "OTHER" ~}
    type: "OTHER"
    other:
      dsn: "${db_dsn}"
%{ else ~}
    type: "ADB-S"
    oci:
      ocid: "${db_ocid}"
%{ endif ~}
    authn:
      secretName: "${db_name}-optimizer-db-authn"
    privAuthn:
      secretName: "${db_name}-db-priv-authn"

client:
  enabled: true
  cookieSecret: "${client_cookie_secret}"
  image:
    repository: ${repository_base}/ai-optimizer-client
    tag: "${app_version}"
    pullPolicy: Always

%{~ if object_storage_bucket != "" }
  oci:
    sourceBucketCompartmentId: "${compartment_id}"
    sourceBucketName: "${object_storage_bucket}"
%{~ endif }

  ingress:
    enabled: true
    className: native-ic
    annotations:
      oci-native-ingress.oraclecloud.com/healthcheck-protocol: HTTP
      oci-native-ingress.oraclecloud.com/healthcheck-path: "/_stcore/health"
      oci-native-ingress.oraclecloud.com/http-listener-port: "80"
  service:
    type: "NodePort"

# -- Self-hosted SigNoz observability backend. The BYO branch routes every
# image via signoz.global.imageRegistry and disables the clickhouse `udf`
# initContainer (it fetches histogram-quantile from github.com at pod start;
# private-registry deployments should not rely on runtime downloads, so
# disabling this removes the histogramQuantile UDF). Reach the UI via:
#   kubectl port-forward -n <ns> svc/<release>-signoz 8080:8080
%{ if is_observability_enabled ~}
signoz:
  enabled: true
  auth:
    existingSecret: true
%{ if ocir_url != "" ~}
  clickhouse:
    initContainers:
      udf:
        enabled: false
%{ endif ~}
%{ endif ~}

ollama:
  enabled: ${install_ollama}
  models:
    enabled: true
    modelPullList:
      - granite4.1:8b
      - mxbai-embed-large
  resources:
    limits:
      nvidia.com/gpu: 1
