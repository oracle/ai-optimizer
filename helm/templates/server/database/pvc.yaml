{{/* Copyright (c) 2026, Oracle and/or its affiliates.
Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.

Persistent storage for SIDB-FREE and ADB-FREE database files. */}}
{{- if and (eq (include "ai-optimizer.server.database.isContainerDB" .) "true") .Values.server.database.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "ai-optimizer.server.database.dataPvcName" . }}
  labels:
    app.kubernetes.io/component: database
    {{- include "ai-optimizer.labels" . | nindent 4 }}
  {{- if not .Values.server.database.persistence.cleanupOnUninstall }}
  annotations:
    helm.sh/resource-policy: keep
  {{- end }}
spec:
  accessModes:
    {{- toYaml .Values.server.database.persistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.server.database.persistence.size | quote }}
  {{- with .Values.server.database.persistence.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
