{{/* Copyright (c) 2024, 2026, Oracle and/or its affiliates.
Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.
spell-checker: ignore nindent freepdb1

Database Deployment (SIDB-FREE or ADB-FREE) */}}
{{- if .Values.server.database }}
{{- if eq (include "ai-optimizer.server.database.isContainerDB" .) "true" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-optimizer.fullname" . }}-{{ include "ai-optimizer.server.database.dbName" . }}
  labels:
    app.kubernetes.io/component: database
    {{- include "ai-optimizer.labels" . | nindent 4}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: database
      {{- include "ai-optimizer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.server.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app.kubernetes.io/component: database
        {{- include "ai-optimizer.labels" . | nindent 8 }}
        {{- with .Values.server.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      securityContext:
        fsGroup: 54321
        runAsGroup: 54321
        runAsUser: 54321
      {{- include "ai-optimizer.imagePullSecrets" (dict "local" .Values.server.imagePullSecrets "global" .Values.global.imagePullSecrets) | nindent 6 }}
      containers:
        - name: db-container
          image: {{ include "ai-optimizer.image" (dict "image" .Values.server.database.image "global" .Values.global) | quote }}
          imagePullPolicy: {{ .Values.server.database.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: 1521
          readinessProbe:
            tcpSocket:
              port: 1521
            initialDelaySeconds: 60
            periodSeconds: 10
          env:
            {{- include "ai-optimizer.server.database.authn" . | nindent 12 }}
          {{- if eq (include "ai-optimizer.server.database.isSIDB" .) "true" }}
            - name: ORACLE_PWD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-optimizer.server.databaseSecret" . }}
                  key: {{ default "password" .Values.server.database.authn.passwordKey }}
          volumeMounts:
            - name: db-init-scripts
              mountPath: "/opt/oracle/scripts/startup"
          {{- else }}
            - name: DATABASE_NAME
              value: FREEPDB1
            - name: ENABLE_ARCHIVE_LOG
              value: "False"
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-optimizer.server.databasePrivSecret" . }}
                  key: {{ default "password" .Values.server.database.privAuthN.passwordKey }}
            - name: WALLET_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "ai-optimizer.server.databaseSecret" . }}
                  key: {{ default "password" .Values.server.database.authn.passwordKey }}
          {{- end }}
      {{- if eq (include "ai-optimizer.server.database.isSIDB" .) "true" }}
      volumes:
        - name: db-init-scripts
          configMap:
            name: {{ include "ai-optimizer.fullname" . }}-db-init
      {{- end }}
{{- end }}
{{- end }}
