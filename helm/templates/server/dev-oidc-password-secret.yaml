{{/* Copyright (c) 2024, 2026, Oracle and/or its affiliates.
Licensed under the Universal Permissive License v1.0 as shown at http://oss.oracle.com/licenses/upl.
spell-checker: ignore nindent */}}

{{- if and .Values.server.devOidc.enabled .Values.server.devOidc.passwordAutoGenerate }}
{{- $secretName := include "ai-optimizer.server.devOidc.passwordSecretName" . }}
{{- $secretKey := include "ai-optimizer.server.devOidc.passwordSecretKey" . }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $password := "" }}
{{- if and $existing $existing.data (hasKey $existing.data $secretKey) }}
  {{- $password = index $existing.data $secretKey | b64dec }}
{{- else }}
  {{- $password = include "ai-optimizer.server.randomPassword" . | trim }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/component: server
    {{- include "ai-optimizer.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  {{ $secretKey }}: {{ $password | quote }}
{{- end }}
